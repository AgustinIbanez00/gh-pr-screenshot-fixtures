# Conflict Fixture

This branch changes `src/conflict.txt` to `release-channel=canary`.

After the PR is created, `main` changes the same line to a different value so
GitHub can report a real merge conflict.
